#!/usr/bin/env bash
set -e

source ${BASH_SOURCE%/*}/env
source ${BASH_SOURCE%/*}/dev-sandbox/env

export NODEPATH=$NODEPATH:$PROJECT_ROOT_DIR/server/src

cd $PROJECT_ROOT_DIR/server

TEZOS_RPC_PORT=$TEZOS_RPC_PORT \
  POSTGRES_PORT=$POSTGRES_PORT \
  npx ts-node scripts/create-sandbox-users.ts
